import { createCommandGroupDecorator } from 'necord';

export const ReactionTriggerCommandDecorator = createCommandGroupDecorator({
	name: 'reaction-trigger',
	description: 'Reaction trigger command group',
});
