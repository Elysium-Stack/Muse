/* tslint:disable */
/* eslint-disable */

export interface TokensResponseDto {

  /**
   * The users's access token.
   */
  accessToken: string;

  /**
   * The users's refresh token.
   */
  refreshToken: string;
}
