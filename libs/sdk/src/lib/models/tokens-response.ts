/* tslint:disable */
/* eslint-disable */
export interface TokensResponse {

  /**
   * The users's access token.
   */
  accessToken: string;

  /**
   * The users's refresh token.
   */
  refreshToken: string;
}
